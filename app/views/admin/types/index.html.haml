%h1 Types

%h3 Info
%p
Available: "Image", 
%p
Maybe create: "ProductHomepage", "ProductCategory", "NormaHomepage",
Reminder: View the model later 
%p
Fields:
Image:(url, caption), 
%p
%h3 List
%ul
  -@types.each do |type|
    %li
      =type.name
      =link_to "edit", edit_admin_type_path(type)
      =link_to "Destroy", admin_type_path(type), :method => :delete, :data => { :confirm => 'Are you sure?' }
      %ul
        -type.field_definitions.each do |field|
          %li=field.key

%p
  =link_to "New Type", new_admin_type_path
